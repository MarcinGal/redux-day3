const INC = 'counter/INC'
const DEC = 'counter/DEC'

const INITIAL_STATE = {
    number: 0
}

export default (state = INITIAL_STATE, action) => {
    switch(action.type){

        default:
        return state
    }
}